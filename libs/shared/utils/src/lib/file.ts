// import { AppConfigEnv } from '@asc-nx/web-config';

export class FileUtil {
    static downloadFile(path: string) {
        // window.open(AppConfigEnv.settings.resourceUrl + path, '_blank');
        return;
    }
}
